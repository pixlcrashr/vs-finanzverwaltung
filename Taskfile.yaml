version: "3"

includes:
  docs: ./docs

tasks:
  release:
    cmds:
      - commit-and-tag-version {{.CLI_ARGS}}
      - git push --follow-tags origin main
  migration:create:
    cmds:
      - atlas migrate diff --env gorm {{.CLI_ARGS}}
