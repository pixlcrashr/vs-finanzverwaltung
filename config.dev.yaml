server:
  addr: "127.0.0.1:8080"
  grpc-addr: "127.0.0.1:9090"

database:
  url: "postgres://vsf:postgres@127.0.0.1:5334/vsf?sslmode=disable"

auth:
  secret: "dev-secret-do-not-use-in-production"
  gitlab:
    client-id: "dev-client-id"
    client-secret: "dev-client-secret"
    issuer: "https://gitlab.com"

gotenberg:
  url: "http://127.0.0.1:3000"

cors:
  enabled: true
  allow-origins:
    - "http://localhost:4200"
    - "http://127.0.0.1:4200"
  allow-methods:
    - "GET"
    - "POST"
    - "PUT"
    - "PATCH"
    - "DELETE"
    - "OPTIONS"
  allow-headers:
    - "Origin"
    - "Content-Type"
    - "Accept"
    - "Authorization"
  allow-credentials: false
  max-age: 300
