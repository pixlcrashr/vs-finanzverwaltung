/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DeleteAccountGroupAssignment$Params {
  accountGroupId: string;
  assignmentId: string;
}

export function deleteAccountGroupAssignment(http: HttpClient, rootUrl: string, params: DeleteAccountGroupAssignment$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, deleteAccountGroupAssignment.PATH, 'delete');
  if (params) {
    rb.path('accountGroupId', params.accountGroupId, {});
    rb.path('assignmentId', params.assignmentId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

deleteAccountGroupAssignment.PATH = '/v1/accountGroups/{accountGroupId}/assignments/{assignmentId}';
