/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListAccountGroupAssignmentsResponseBody } from '../../models/list-account-group-assignments-response-body';

export interface ListAccountGroupAssignments$Params {
  accountGroupId: string;
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Filter by account ID
 */
  accountId?: string;

/**
 * Filter by negate flag
 */
  negate?: boolean;

/**
 * Order by field (e.g. 'createTime desc')
 */
  orderBy?: string;
}

export function listAccountGroupAssignments(http: HttpClient, rootUrl: string, params: ListAccountGroupAssignments$Params, context?: HttpContext): Observable<StrictHttpResponse<ListAccountGroupAssignmentsResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listAccountGroupAssignments.PATH, 'get');
  if (params) {
    rb.path('accountGroupId', params.accountGroupId, {});
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('accountId', params.accountId, {"explode":false});
    rb.query('negate', params.negate, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListAccountGroupAssignmentsResponseBody>;
    })
  );
}

listAccountGroupAssignments.PATH = '/v1/accountGroups/{accountGroupId}/assignments';
