/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AccountGroupAssignment } from '../../models/account-group-assignment';
import { UpdateAccountGroupAssignmentRequestBody } from '../../models/update-account-group-assignment-request-body';

export interface UpdateAccountGroupAssignment$Params {
  accountGroupId: string;
  assignmentId: string;
      body: UpdateAccountGroupAssignmentRequestBody
}

export function updateAccountGroupAssignment(http: HttpClient, rootUrl: string, params: UpdateAccountGroupAssignment$Params, context?: HttpContext): Observable<StrictHttpResponse<AccountGroupAssignment>> {
  const rb = new RequestBuilder(rootUrl, updateAccountGroupAssignment.PATH, 'patch');
  if (params) {
    rb.path('accountGroupId', params.accountGroupId, {});
    rb.path('assignmentId', params.assignmentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccountGroupAssignment>;
    })
  );
}

updateAccountGroupAssignment.PATH = '/v1/accountGroups/{accountGroupId}/assignments/{assignmentId}';
