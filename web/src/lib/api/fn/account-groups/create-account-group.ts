/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AccountGroup } from '../../models/account-group';
import { CreateAccountGroupRequestBody } from '../../models/create-account-group-request-body';

export interface CreateAccountGroup$Params {
      body: CreateAccountGroupRequestBody
}

export function createAccountGroup(http: HttpClient, rootUrl: string, params: CreateAccountGroup$Params, context?: HttpContext): Observable<StrictHttpResponse<AccountGroup>> {
  const rb = new RequestBuilder(rootUrl, createAccountGroup.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AccountGroup>;
    })
  );
}

createAccountGroup.PATH = '/v1/accountGroups';
