/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListAccountsResponseBody } from '../../models/list-accounts-response-body';

export interface ListAccounts$Params {

/**
 * Accounts per page (max 100)
 */
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Filter by display name prefix
 */
  displayName?: string;

/**
 * Include archived accounts (default false)
 */
  showDeleted?: boolean;

/**
 * Order by field (e.g. 'displayName', 'displayCode', 'createTime desc')
 */
  orderBy?: string;
}

export function listAccounts(http: HttpClient, rootUrl: string, params?: ListAccounts$Params, context?: HttpContext): Observable<StrictHttpResponse<ListAccountsResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listAccounts.PATH, 'get');
  if (params) {
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('displayName', params.displayName, {"explode":false});
    rb.query('showDeleted', params.showDeleted, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListAccountsResponseBody>;
    })
  );
}

listAccounts.PATH = '/v1/accounts';
