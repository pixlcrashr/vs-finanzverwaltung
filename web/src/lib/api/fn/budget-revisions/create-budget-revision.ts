/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BudgetRevision } from '../../models/budget-revision';
import { CreateBudgetRevisionRequestBody } from '../../models/create-budget-revision-request-body';

export interface CreateBudgetRevision$Params {

/**
 * Budget UUID
 */
  budgetId: string;
      body: CreateBudgetRevisionRequestBody
}

export function createBudgetRevision(http: HttpClient, rootUrl: string, params: CreateBudgetRevision$Params, context?: HttpContext): Observable<StrictHttpResponse<BudgetRevision>> {
  const rb = new RequestBuilder(rootUrl, createBudgetRevision.PATH, 'post');
  if (params) {
    rb.path('budgetId', params.budgetId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BudgetRevision>;
    })
  );
}

createBudgetRevision.PATH = '/v1/budgets/{budgetId}/revisions';
