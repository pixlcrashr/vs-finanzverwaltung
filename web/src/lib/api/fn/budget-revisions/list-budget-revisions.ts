/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListBudgetRevisionsResponseBody } from '../../models/list-budget-revisions-response-body';

export interface ListBudgetRevisions$Params {

/**
 * Budget UUID
 */
  budgetId: string;

/**
 * Revisions per page (max 100)
 */
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Order by field (e.g. 'date desc', 'createTime')
 */
  orderBy?: string;
}

export function listBudgetRevisions(http: HttpClient, rootUrl: string, params: ListBudgetRevisions$Params, context?: HttpContext): Observable<StrictHttpResponse<ListBudgetRevisionsResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listBudgetRevisions.PATH, 'get');
  if (params) {
    rb.path('budgetId', params.budgetId, {});
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListBudgetRevisionsResponseBody>;
    })
  );
}

listBudgetRevisions.PATH = '/v1/budgets/{budgetId}/revisions';
