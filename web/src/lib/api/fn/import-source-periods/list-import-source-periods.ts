/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListImportSourcePeriodsResponseBody } from '../../models/list-import-source-periods-response-body';

export interface ListImportSourcePeriods$Params {
  importSourceId: string;
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Order by field (e.g. 'year desc', 'createTime')
 */
  orderBy?: string;
}

export function listImportSourcePeriods(http: HttpClient, rootUrl: string, params: ListImportSourcePeriods$Params, context?: HttpContext): Observable<StrictHttpResponse<ListImportSourcePeriodsResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listImportSourcePeriods.PATH, 'get');
  if (params) {
    rb.path('importSourceId', params.importSourceId, {});
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListImportSourcePeriodsResponseBody>;
    })
  );
}

listImportSourcePeriods.PATH = '/v1/importSources/{importSourceId}/periods';
