/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ImportSource } from '../../models/import-source';
import { UpdateImportSourceRequestBody } from '../../models/update-import-source-request-body';

export interface UpdateImportSource$Params {

/**
 * Import source UUID
 */
  importSourceId: string;
      body: UpdateImportSourceRequestBody
}

export function updateImportSource(http: HttpClient, rootUrl: string, params: UpdateImportSource$Params, context?: HttpContext): Observable<StrictHttpResponse<ImportSource>> {
  const rb = new RequestBuilder(rootUrl, updateImportSource.PATH, 'patch');
  if (params) {
    rb.path('importSourceId', params.importSourceId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ImportSource>;
    })
  );
}

updateImportSource.PATH = '/v1/importSources/{importSourceId}';
