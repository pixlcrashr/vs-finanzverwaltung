/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateReportTemplateRequestBody } from '../../models/create-report-template-request-body';
import { ReportTemplate } from '../../models/report-template';

export interface CreateReportTemplate$Params {
      body: CreateReportTemplateRequestBody
}

export function createReportTemplate(http: HttpClient, rootUrl: string, params: CreateReportTemplate$Params, context?: HttpContext): Observable<StrictHttpResponse<ReportTemplate>> {
  const rb = new RequestBuilder(rootUrl, createReportTemplate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReportTemplate>;
    })
  );
}

createReportTemplate.PATH = '/v1/reportTemplates';
