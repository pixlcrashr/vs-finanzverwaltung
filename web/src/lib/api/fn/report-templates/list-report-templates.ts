/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListReportTemplatesResponseBody } from '../../models/list-report-templates-response-body';

export interface ListReportTemplates$Params {

/**
 * Templates per page (max 100)
 */
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Filter by name prefix
 */
  displayName?: string;

/**
 * Order by field (e.g. 'displayName', 'createTime desc')
 */
  orderBy?: string;
}

export function listReportTemplates(http: HttpClient, rootUrl: string, params?: ListReportTemplates$Params, context?: HttpContext): Observable<StrictHttpResponse<ListReportTemplatesResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listReportTemplates.PATH, 'get');
  if (params) {
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('displayName', params.displayName, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListReportTemplatesResponseBody>;
    })
  );
}

listReportTemplates.PATH = '/v1/reportTemplates';
