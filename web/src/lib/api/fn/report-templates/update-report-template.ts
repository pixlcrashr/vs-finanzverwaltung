/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReportTemplate } from '../../models/report-template';
import { UpdateReportTemplateRequestBody } from '../../models/update-report-template-request-body';

export interface UpdateReportTemplate$Params {

/**
 * Report template UUID
 */
  reportTemplateId: string;
      body: UpdateReportTemplateRequestBody
}

export function updateReportTemplate(http: HttpClient, rootUrl: string, params: UpdateReportTemplate$Params, context?: HttpContext): Observable<StrictHttpResponse<ReportTemplate>> {
  const rb = new RequestBuilder(rootUrl, updateReportTemplate.PATH, 'patch');
  if (params) {
    rb.path('reportTemplateId', params.reportTemplateId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReportTemplate>;
    })
  );
}

updateReportTemplate.PATH = '/v1/reportTemplates/{reportTemplateId}';
