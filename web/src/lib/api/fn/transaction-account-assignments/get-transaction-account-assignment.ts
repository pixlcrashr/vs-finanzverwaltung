/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TransactionAccountAssignment } from '../../models/transaction-account-assignment';

export interface GetTransactionAccountAssignment$Params {
  transactionId: string;
  assignmentId: string;
}

export function getTransactionAccountAssignment(http: HttpClient, rootUrl: string, params: GetTransactionAccountAssignment$Params, context?: HttpContext): Observable<StrictHttpResponse<TransactionAccountAssignment>> {
  const rb = new RequestBuilder(rootUrl, getTransactionAccountAssignment.PATH, 'get');
  if (params) {
    rb.path('transactionId', params.transactionId, {});
    rb.path('assignmentId', params.assignmentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TransactionAccountAssignment>;
    })
  );
}

getTransactionAccountAssignment.PATH = '/v1/transactions/{transactionId}/assignments/{assignmentId}';
