/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListTransactionAccountAssignmentsResponseBody } from '../../models/list-transaction-account-assignments-response-body';

export interface ListTransactionAccountAssignments$Params {
  transactionId: string;
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Order by field (e.g. 'createTime desc')
 */
  orderBy?: string;
}

export function listTransactionAccountAssignments(http: HttpClient, rootUrl: string, params: ListTransactionAccountAssignments$Params, context?: HttpContext): Observable<StrictHttpResponse<ListTransactionAccountAssignmentsResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listTransactionAccountAssignments.PATH, 'get');
  if (params) {
    rb.path('transactionId', params.transactionId, {});
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListTransactionAccountAssignmentsResponseBody>;
    })
  );
}

listTransactionAccountAssignments.PATH = '/v1/transactions/{transactionId}/assignments';
