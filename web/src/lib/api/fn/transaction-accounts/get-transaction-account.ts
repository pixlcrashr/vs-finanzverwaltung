/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TransactionAccount } from '../../models/transaction-account';

export interface GetTransactionAccount$Params {

/**
 * Transaction account UUID
 */
  transactionAccountId: string;
}

export function getTransactionAccount(http: HttpClient, rootUrl: string, params: GetTransactionAccount$Params, context?: HttpContext): Observable<StrictHttpResponse<TransactionAccount>> {
  const rb = new RequestBuilder(rootUrl, getTransactionAccount.PATH, 'get');
  if (params) {
    rb.path('transactionAccountId', params.transactionAccountId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TransactionAccount>;
    })
  );
}

getTransactionAccount.PATH = '/v1/transactionAccounts/{transactionAccountId}';
