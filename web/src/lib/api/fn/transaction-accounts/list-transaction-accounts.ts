/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListTransactionAccountsResponseBody } from '../../models/list-transaction-accounts-response-body';

export interface ListTransactionAccounts$Params {

/**
 * Accounts per page (max 100)
 */
  pageSize?: number;

/**
 * Page number (1-indexed)
 */
  page?: number;

/**
 * Filter by import source ID
 */
  importSourceId?: string;

/**
 * Filter by code prefix
 */
  code?: string;

/**
 * Order by field (e.g. 'code', 'displayName', 'createTime desc')
 */
  orderBy?: string;
}

export function listTransactionAccounts(http: HttpClient, rootUrl: string, params?: ListTransactionAccounts$Params, context?: HttpContext): Observable<StrictHttpResponse<ListTransactionAccountsResponseBody>> {
  const rb = new RequestBuilder(rootUrl, listTransactionAccounts.PATH, 'get');
  if (params) {
    rb.query('pageSize', params.pageSize, {"explode":false});
    rb.query('page', params.page, {"explode":false});
    rb.query('importSourceId', params.importSourceId, {"explode":false});
    rb.query('code', params.code, {"explode":false});
    rb.query('orderBy', params.orderBy, {"explode":false});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListTransactionAccountsResponseBody>;
    })
  );
}

listTransactionAccounts.PATH = '/v1/transactionAccounts';
