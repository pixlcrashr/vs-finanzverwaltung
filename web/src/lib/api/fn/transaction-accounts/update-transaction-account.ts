/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TransactionAccount } from '../../models/transaction-account';
import { UpdateTransactionAccountRequestBody } from '../../models/update-transaction-account-request-body';

export interface UpdateTransactionAccount$Params {

/**
 * Transaction account UUID
 */
  transactionAccountId: string;
      body: UpdateTransactionAccountRequestBody
}

export function updateTransactionAccount(http: HttpClient, rootUrl: string, params: UpdateTransactionAccount$Params, context?: HttpContext): Observable<StrictHttpResponse<TransactionAccount>> {
  const rb = new RequestBuilder(rootUrl, updateTransactionAccount.PATH, 'patch');
  if (params) {
    rb.path('transactionAccountId', params.transactionAccountId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TransactionAccount>;
    })
  );
}

updateTransactionAccount.PATH = '/v1/transactionAccounts/{transactionAccountId}';
